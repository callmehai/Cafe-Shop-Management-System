import 'package:flutter/material.dart';

/// Bảng màu trích từ thiết kế Figma "CSMS Mobile Screens" (tông cà phê + kem).
class AppColors {
  AppColors._();

  static const cream = Color(0xFFF2EBE3); // nền scaffold
  static const surface = Color(0xFFFBF8F4); // card sáng
  static const surfaceAlt = Color(0xFFFFFFFF);

  static const espresso = Color(0xFF3A2D26); // card header tối (revenue)
  static const espressoDeep = Color(0xFF2E2521);

  static const terracotta = Color(0xFFC2683D); // nút hành động chính
  static const terracottaDark = Color(0xFFA8522C);

  static const sage = Color(0xFFDCE3D8); // card phụ tông xanh rêu nhạt
  static const sageText = Color(0xFF5E7355);

  static const textPrimary = Color(0xFF2E2521);
  static const textMuted = Color(0xFF8C7F74);
  static const border = Color(0xFFE7DECF);

  static const danger = Color(0xFFB3402F);
}
